<Page
    x:Class="WinUITestParser.MVVM.View.EditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:brushes="using:CommunityToolkit.WinUI.UI.Media"
    mc:Ignorable="d">

    <Grid Padding="5" ColumnSpacing="5">

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Column="0">

            <Grid.Resources>
                <CommandBarFlyout x:Name="OrDocumentCommandsFlyout">
                    <CommandBarFlyout.SecondaryCommands>

                        <AppBarButton Label="Открыть" Click="OpenOrBtn_Click">
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE7C3;"/>
                        </AppBarButton>

                        <AppBarButton Label="Сохранить" Icon="Save" Click="SaveOrBtn_Click"/>

                        <AppBarButton Label="Форматировать" Click="FormatOrBtn_Click" >
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8A4;"/>
                        </AppBarButton>

                        <AppBarButton Label="Валидировать" Click="ValidateOrBtn_Click" >
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xF87B;"/>
                        </AppBarButton>

                        <AppBarButton Label="Анализировать" Click="AnalyzOrBtn_Click" >
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xECAA;"/>
                        </AppBarButton>

                        <AppBarSeparator/>

                        <AppBarButton Label="Создать перевод" Click="CreateTranslateBtn_Click" >
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xF2B7;"/>
                        </AppBarButton>

                        <AppBarButton Label="Сравнить" Click="CheckMatchesBtn_Click" >
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE73A;"/>
                        </AppBarButton>

                    </CommandBarFlyout.SecondaryCommands>

                </CommandBarFlyout>
            </Grid.Resources>

            <RichEditBox 
                x:Name="editor1" 
                Margin="0 0 0 35"   
                ScrollViewer.VerticalScrollMode="Enabled"
                ScrollViewer.VerticalScrollBarVisibility="Visible"
                ScrollViewer.HorizontalScrollBarVisibility="Visible" 
                ScrollViewer.HorizontalScrollMode="Enabled"
                TextWrapping="NoWrap" 
                CanBeScrollAnchor="True"
                IsSpellCheckEnabled="False"
                IsTextPredictionEnabled="False"
                IsColorFontEnabled="False"
                FlyoutBase.AttachedFlyout="{x:Bind OrDocumentCommandsFlyout}"
                SelectionFlyout="{x:Bind OrDocumentCommandsFlyout}"
                ContextFlyout="{x:Bind OrDocumentCommandsFlyout}"/>

            <StackPanel x:Name="OrSubPanel" VerticalAlignment="Bottom" Height="30" Padding="5" BorderBrush="DimGray" BorderThickness="1" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

                <Grid Height="18" Padding="5 0">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition></ColumnDefinition>
                        <ColumnDefinition></ColumnDefinition>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" HorizontalAlignment="Left" Orientation="Horizontal" Spacing="5" >
                        <Image VerticalAlignment="Center" Height="15" Width="15" Source="Assets/StatusIcon/error.png"/>
                        <TextBlock Text="{x:Bind OriginValidation.CountError, Mode=OneWay}"/>
                        <Image VerticalAlignment="Center" Height="15" Width="15" Source="Assets/StatusIcon/warning.png"/>
                        <TextBlock Text="{x:Bind OriginValidation.CountWarning, Mode=OneWay}"/>
                    </StackPanel>

                    <Button  Grid.Column="1" HorizontalAlignment="Right"  Padding="10 5" Click="OrSubPanelClose_Click">
                        <FontIcon x:Name="OrSubPanelCloseIcon" FontSize="10" Glyph="&#xE971;" />
                    </Button>

                </Grid>

                <ScrollViewer Height="118">
                    <ListView ItemsSource="{x:Bind OriginValidation.Errors, Mode=TwoWay}" SelectionMode="None" IsItemClickEnabled="True" ItemClick="ViewOrErrorItem_Click" >
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnSpacing="5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="20"></ColumnDefinition>
                                        <ColumnDefinition></ColumnDefinition>
                                    </Grid.ColumnDefinitions>
                                    <Image VerticalAlignment="Center" Height="15" Width="15" Source="{Binding IconPath}"/>
                                    <TextBlock VerticalAlignment="Center" FontSize="12" Grid.Column="1" Text="{Binding Message }" TextWrapping="Wrap"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </ScrollViewer>

            </StackPanel>

            <controls:Loading IsLoading="{x:Bind IsOriginLoading, Mode=OneWay}">
                <controls:Loading.Background>
                    <brushes:AcrylicBrush TintColor="Black" TintOpacity="0.63" BlurAmount="2"/>
                </controls:Loading.Background>
                <ContentControl>
                    <StackPanel>
                        <ProgressRing IsActive="True" Foreground="Green" Width="50" Height="50"  />
                    </StackPanel>
                </ContentControl>
            </controls:Loading>

        </Grid>

        <Grid Grid.Column="1">

            <Grid.Resources>
                <CommandBarFlyout x:Name="TrDocumentCommandsFlyout">
                    <CommandBarFlyout.SecondaryCommands>

                        <AppBarButton Label="Открыть" Click="OpenTrBtn_Click">
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE7C3;"/>
                        </AppBarButton>

                        <AppBarButton Label="Сохранить" Icon="Save" Click="SaveTrBtn_Click"/>

                        <AppBarButton Label="Форматировать" Click="FormatTrBtn_Click" >
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8A4;"/>
                        </AppBarButton>

                        <AppBarButton Label="Валидировать" Click="ValidateTrBtn_Click" >
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8D3;"/>
                        </AppBarButton>

                        <AppBarButton Label="Анализировать" Click="AnalyzTrBtn_Click" >
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xECAA;"/>
                        </AppBarButton>

                        <AppBarSeparator/>

                        <AppBarButton Label="Актуализировать" Click="UpdateTrBtn_Click" >
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE72C;"/>
                        </AppBarButton>

                    </CommandBarFlyout.SecondaryCommands>
                </CommandBarFlyout>
            </Grid.Resources>

            <RichEditBox 
                x:Name="editor2" 
                Margin="0 0 0 35"   
                ScrollViewer.VerticalScrollMode="Enabled"
                ScrollViewer.VerticalScrollBarVisibility="Visible"
                ScrollViewer.HorizontalScrollBarVisibility="Visible" 
                ScrollViewer.HorizontalScrollMode="Enabled"
                TextWrapping="NoWrap" 
                CanBeScrollAnchor="True"
                IsSpellCheckEnabled="False"
                IsTextPredictionEnabled="False"
                IsColorFontEnabled="False"
                FlyoutBase.AttachedFlyout="{x:Bind TrDocumentCommandsFlyout}"
                SelectionFlyout="{x:Bind TrDocumentCommandsFlyout}"
                ContextFlyout="{x:Bind TrDocumentCommandsFlyout}"/>

            <StackPanel x:Name="TrSubPanel" VerticalAlignment="Bottom" Height="30" Padding="5" BorderBrush="DimGray" BorderThickness="1" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

                <Grid Height="18" Padding="5 0">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition></ColumnDefinition>
                        <ColumnDefinition></ColumnDefinition>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" HorizontalAlignment="Left" Orientation="Horizontal" Spacing="5" >
                        <Image VerticalAlignment="Center" Height="15" Width="15" Source="Assets/StatusIcon/error.png"/>
                        <TextBlock Text="{x:Bind TranslateValidation.CountError, Mode=OneWay}"/>
                        <Image VerticalAlignment="Center" Height="15" Width="15" Source="Assets/StatusIcon/warning.png"/>
                        <TextBlock Text="{x:Bind TranslateValidation.CountWarning, Mode=OneWay}"/>
                    </StackPanel>

                    <Button  Grid.Column="1" HorizontalAlignment="Right"  Padding="10 5" Click="TrSubPanelClose_Click">
                        <FontIcon x:Name="TrSubPanelCloseIcon" FontSize="10" Glyph="&#xE971;" />
                    </Button>

                </Grid>

                <ScrollViewer Height="118">
                    <ListView ItemsSource="{x:Bind TranslateValidation.Errors, Mode=TwoWay}" SelectionMode="None" IsItemClickEnabled="True" ItemClick="ViewTrErrorItem_Click" >
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnSpacing="5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="20"></ColumnDefinition>
                                        <ColumnDefinition></ColumnDefinition>
                                    </Grid.ColumnDefinitions>
                                    <Image VerticalAlignment="Center" Height="15" Width="15" Source="{Binding IconPath}"/>
                                    <TextBlock VerticalAlignment="Center" FontSize="12" Grid.Column="1" Text="{Binding Message }" TextWrapping="Wrap"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </ScrollViewer>

            </StackPanel>

            <controls:Loading IsLoading="{x:Bind IsTranslateLoading, Mode=OneWay}">
                <controls:Loading.Background>
                    <brushes:AcrylicBrush TintColor="Black" TintOpacity="0.63" BlurAmount="2"/>
                </controls:Loading.Background>
                <ContentControl>
                    <StackPanel>
                        <ProgressRing IsActive="True" Foreground="Green" Width="50" Height="50"  />
                    </StackPanel>
                </ContentControl>
            </controls:Loading>

        </Grid>

    </Grid>


</Page>
